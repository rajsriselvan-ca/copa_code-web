{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://weak-red-xerus-slip.cyclic.app",
            "webRoot": "${workspaceFolder}"
        }
    ]
}